# 2026-03-29 - Mind Mapp Session

## What happened
- Fixed wildcard search bug: suffix patterns like `alp*` and `*review` now work
- Root cause: anchor logic in `globToRegex` was adding `^` prefix for suffix wildcards
- Removed anchor logic entirely - regex is used for substring matching, not exact matching
- All 428 tests passing
- Cleaned up stale debug test files
- Committed: `fix: wildcard search suffix/prefix patterns now work correctly`

## State
- Mind Mapp v0.13.0 on branch `feature/node-tags`
- All MVP features complete
- Wildcard search fixed

## Backlog Status
- Future items remaining: Collaborative editing, Plugin system, Mobile app, Advanced search (regex), Export templates, Cloud sync, Presentation mode, Custom themes, Keyboard customization, Animations, Advanced filters

## Next session could work on
- E2E testing setup with Playwright (already in devDependencies)
- Browser compatibility testing
- Or move to a different project

## Mind Mapp — Session 2 (2026-03-29 ~22:10 UTC)

### What happened
- Implemented **Custom Themes** feature (next backlog item)
- 7 presets: Light, Paper, Mint, Rose (light), Dark, Nord, Solarized (dark)
- Created ThemeDialog.tsx — modal with live preview, click to preview, double-click or Apply to confirm
- Shift+T now opens theme dialog instead of binary light/dark toggle
- New `themePresets.ts` persists selected preset to localStorage (key: `mindmapp.v0.2.theme`)
- `loadTheme()` in `theme.ts` reads new preset key, falls back to v0.1 key for legacy
- Refactored App.tsx: replaced `theme: Theme` state with `themePresetId: string`
- `StyleToolbar` prop updated to derive `isDark` from current preset
- All 449 tests passing (up from 438)

### Changes
- `app/src/utils/themePresets.ts` — new module with 7 presets + applyPreset/savePreset/loadSavedPreset
- `app/src/utils/themePresets.test.ts` — 14 tests for new module
- `app/src/utils/theme.ts` — simplified, removed toggleTheme/saveTheme (moved to presets)
- `app/src/utils/theme.test.ts` — updated tests for new API
- `app/src/components/ThemeDialog.tsx` — new dialog component
- `app/src/App.tsx` — wired in ThemeDialog, Shift+T opens dialog
- `BACKLOG.md` — marked "Custom themes" as done

### State
- Branch: feature/node-tags
- Commit: 7ab171c — "feat: custom themes — 7 presets (Light, Paper, Mint, Rose, Dark, Nord, Solarized)"
- Remote: not configured (push would need credentials)

### Still on backlog
- Keyboard shortcut customization
- Node animations and transitions
- Advanced filters (by style, date, properties)
- Collaborative editing (CRDT)
- Plugin system
- Mobile app
- Cloud sync
- File attachments (non-image binary)
- Presentation mode
- Advanced search (regex)
- Export templates

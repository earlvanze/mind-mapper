# 2026-03-26 - Mind Mapp Build Session

## Session Start: 01:40 UTC

## What Happened
1. **Ran tests** — 3 failures in treeLayout.test.ts (290 tests total)
   - Root cause: tests assumed vertical tree layout but algorithm is horizontal (depth on Y, siblings on X)
2. **Fixed treeLayout tests** — updated 3 assertions to match actual layout behavior
   - `expect(result.a.y).toBe(result.root.y)` → removed (Y increases with depth)
   - `expect(a.x < b.x)` → `expect(Math.max(...xs) > Math.min(...xs))` for horizontal spread
   - `expect unique x` → `expect depth-1 same Y, depth-2 greater Y`
3. **All 290 tests pass** ✅
4. **Build succeeds** ✅ (chunk size warning only, pre-existing)
5. **Committed fix** as `10d0393`

## Project State
- v0.5 complete (PDF, SVG, FreeMind export)
- v0.4 complete (node styling: colors, shapes, icons)
- v0.3 complete (canvas renderer, virtualization)
- All MVP + Should-Do + Could-Do items done
- "Future Enhancements" in BACKLOG.md: most already implemented (auto-layout, rich text, etc. exist in some form)
- Remaining real future items: collaborative editing CRDTs, plugin system, mobile app

## Next Session Priorities
- Pick from Future Enhancements or do a polish/stretch pass
- Consider implementing tree/radial/force-directed auto-layout toggle (L key exists, but only does basic vertical)
- Rich text editing (bold/italic in nodes) — flagged as future but no implementation

## Session End: 02:57 UTC

### What Happened
1. **Ran tests** — 290/290 pass ✅
2. **Built rich text editing (bold + italic)** — v0.6 feature added:
   - `NodeStyle`: `bold?: boolean`, `italic?: boolean` fields added
   - `nodeStyles.ts`: `resolveStyle()` returns `bold`/`italic` (default false)
   - `CanvasRenderer.tsx`: ctx.font built with bold/italic style; cache key includes font style
   - `Node.tsx`: full rewrite — inline B/I toolbar floats above node when editing; `onKeyDown` handles Cmd+B/I; display bold/italic driven by live state
   - `StyleToolbar.tsx`: Bold + Italic toggle buttons added with active state highlighting
   - `useKeyboard.ts`: Cmd+B/I handlers when `editingId` is set
   - `shortcuts.ts`: documented new shortcuts
   - `importValidation.ts`: accepts `bold`/`italic` boolean in node style
3. **Build succeeds** ✅
4. **Committed** as `94f447f`

### Implementation Notes
- Editing bold/italic uses local state (`editingBold`, `editingItalic`) synced from node.style on edit start
- `setSelectedStyle` called directly (no history commit) for inline editing responsiveness
- Canvas renderer: bold+italic combined = "italic bold" (italic + bold cascade in CSS)
- Import validation: `node.style?.bold == null || typeof node.style.bold === 'boolean'` — accepts absent/undefined or boolean

### Next Session
- All MVP + Should-Do + Could-Do + v0.3/0.4/0.5 + Rich Text done
- Remaining Future Enhancements: collaborative editing CRDTs, plugin system, mobile app, auto-layout algorithms, attachments, version history
- Consider auto-layout toggle (tree/radial/force-directed) as next real feature

## Session Start: 03:58 UTC

### What Happened
1. **Ran tests** — 290/290 pass ✅
2. **Built multiple auto-layout algorithms (v0.7 feature):**
   - `treeLayout.ts`: refactored to export `computeLayout(rootId, nodes, mode)` + `LayoutMode` type
   - **Tree layout**: Reingold-Tilford compact tree (existing algorithm, now properly wired)
   - **Radial layout**: BFS-based concentric circles, root at centre, RADIAL_RADIUS=180px, RADIAL_GAP=16px arc gap between siblings
   - Positions normalised to centroid=(0,0), then offset to keep focus node in place
   - `MindMapState`: added `layoutMode: LayoutMode` + `autoLayout(rootId?)` + `setLayoutMode(mode)`
   - `useKeyboard.ts`: 'L' key now cycles tree↔radial then applies layout (replaces old `autoLayoutChildren` which only spread immediate children in a simple row)
3. **Build succeeds** ✅
4. **Updated shortcuts.ts** — L shortcut description updated
5. **Updated BACKLOG.md** — Auto-layout marked complete
6. **Committed** as `d0a17f9`

### Implementation Notes
- Old `autoLayoutChildren` still exists (only spreads immediate children in a simple vertical row) but is no longer on any key
- `autoLayout` uses `computeLayout` which calls `normalise()` — keeps the focused subtree centred on the focus node's current position
- Radial layout: angle range per child = parent_angle_range / child_count; arc gap applied proportionally to radius

### Project State
- v0.7: Auto-layout algorithms (tree + radial) ✅ NEW
- All MVP + Should-Do + Could-Do + v0.3/0.4/0.5 + Rich Text + Auto-layout done
- Future Enhancements remaining: collaborative editing CRDTs, plugin system, mobile app, force-directed layout, attachments, version history

## Session End: 04:09 UTC

## 2026-03-26 - Mind Mapp Build Session (09:34 UTC)

### What Happened
1. **Ran tests** — 290/290 pass ✅
2. **Build succeeds** ✅ (chunk size warning only, pre-existing)
3. **Built force-directed layout (v0.8):**
   - `src/utils/treeLayout.ts`: Added `computeForceLayout()` — Fruchterman-Reingold physics sim
     - 60 iterations, Coulomb repulsion (k²=8000), Hooke springs (k=0.05, L=200px)
     - Weak gravity toward origin to keep centred
     - Deterministic RNG for reproducible initial positions
     - Damping (0.85) + temperature annealing (200→1)
   - `LayoutMode`: extended to `'tree' | 'radial' | 'force'`
   - `computeLayout()`: dispatches to force layout when mode='force'
   - `useKeyboard.ts`: L key now cycles tree→radial→force→tree
   - `shortcuts.ts`: updated L shortcut description
4. **Committed** as `530c279`
5. **Updated BACKLOG.md** — auto-layout now includes force-directed

### Remaining Future Enhancements
- [ ] Collaborative editing (CRDTs)
- [ ] Plugin system
- [ ] Mobile app (React Native)
- [ ] Rich text editing (bold, italic, lists) — bold/italic code was removed in earlier rollback
- [ ] Attachments (images, files, links)
- [ ] Version history and branching

## Session End: 09:36 UTC

## 10:40 - Mind Mapp Session (tech-mvp agent)

### Tests & Build
- All 290 tests passing ✅
- Build successful ✅ (chunk size warning, non-blocking)

### Version Gap Fix
Discovered the codebase was at v0.8.0 in code but stuck at v0.3.0 in docs/package.json. Features were already committed but release notes, changelog, and roadmap were never updated.

### Changes Committed
- `app/package.json`: version 0.3.0 → 0.8.0
- `app/CHANGELOG.md`: added 0.4.0, 0.5.0, 0.8.0 entries
- `app/RELEASE_NOTES_0.4.0.md` (new): node styling features
- `app/RELEASE_NOTES_0.5.0.md` (new): PDF/SVG/FreeMind export
- `app/RELEASE_NOTES_0.8.0.md` (new): tree/radial/force auto-layout
- `app/ROADMAP.md`: updated to reflect v0.4, v0.5, v0.8 complete; v0.6/v1.0 remaining
- `app/KNOWN_ISSUES.md`: cleaned up resolved items
- `app/TODO.md`: moved to v0.6/v0.7 priorities

### Current State
- **v0.8.0** fully shipped: MVP + v0.2 features + performance + node styling + enhanced exports + auto-layout
- Next real work item: **v0.6 Collaborative editing (CRDT)**

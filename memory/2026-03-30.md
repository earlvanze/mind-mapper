# 2026-03-30 - Mind Mapp Session

## What happened
- **Tests:** 484/484 passing ✅
- **Build:** succeeds cleanly (v0.13.1) ✅
- **Commits:** 1 (affddfe) — "feat: advanced filters — shape, color, icon, date + FilterPanel refactor"

### v0.13.1 — Advanced Filters + FilterPanel Refactor ✅

#### New filter system (nodeFilters.ts + nodeFilters.test.ts)
- `shouldFadeNode()` — unified filter logic for tags/shapes/colors/icons/dates
- `getUniqueShapes()`, `getUniqueColors()`, `getUniqueIcons()` — collect unique values
- 46 tests covering all filter paths

#### FilterPanel.tsx (replaces TagFilterPanel)
- **Tags tab:** tag filtering with search, match mode (any/all), active chips
- **Style tab:** filter by shape, color, icon — multi-select
- **Date tab:** filter by created/updated date with from/to range
- Clears visual feedback + Clear All button

#### Store additions (useMindMapStore.ts)
- `styleFilterShapes: string[]`
- `styleFilterColors: string[]`
- `styleFilterIcons: string[]`
- `styleFilterDateMode?: 'created' | 'updated'`
- `styleFilterDateFrom/To?: number`
- `setStyleFilterShapes/Colors/Icons/Date()`, `clearStyleFilters()`

#### Node timestamps
- `createdAt` and `updatedAt` set on node create/update
- Enables date-based filtering

#### Keyboard shortcuts wiring
- `checkCustomBinding()` — detect custom bindings from localStorage
- `ACTION_TO_HANDLER` map — route action → handler name
- `useKeyboard.ts` — dispatch to registered handlers

#### Fixes
- Color filter substring matching (`'255'` matches `rgba(255,0,0,0.5)`)
- `getUniqueShapes()` returns `['rectangle']` for nodes without explicit shape (default)

### Backlog Status
- [x] Advanced filters (shape, color, icon, date) ✅ NEW — moved from Future
- [x] Node tags/categories ✅ (was already done)
- [ ] Keyboard shortcut customization (partially done — wiring exists)
- [ ] Node animations and transitions

### Still on Future backlog
- Collaborative editing (CRDT-based)
- Plugin system for custom visualizations
- Mobile app (React Native)
- File attachments (non-image binary files)
- Advanced search (regex, wildcards)
- Export templates (custom styling presets)
- Cloud sync (optional backend)
- Presentation mode (slideshow through branches)
- Keyboard shortcut customization
- Node animations and transitions

### State
- Branch: feature/node-tags
- Commit: affddfe
- Version: v0.13.1
